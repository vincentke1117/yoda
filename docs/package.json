{
  "name": "yoda-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 3000",
    "build": "vite build && node -e \"require('node:fs').cpSync('images', 'dist/images', { recursive: true })\"",
    "preview": "vite preview --host 127.0.0.1 --port 3000"
  }
}
